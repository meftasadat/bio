/**
 * Feature Flags Configuration
 * 
 * Use this file to toggle features on/off across the application.
 * Set a flag to `true` to enable a feature, `false` to disable it.
 */

export const FEATURE_FLAGS = {
    // When false, hides the Apps section from the top navigation bar
    SHOW_APPS_NAV: false,
};
